namespace ScreenSaver
{
    internal enum DstIndicatorStyle
    {
        SmallDst,  // Small "DST" in top half, and day diff shown as -1d or +1d in bottom half
        DstAsAsterisk  // If day different, shows as mmm
    }
}